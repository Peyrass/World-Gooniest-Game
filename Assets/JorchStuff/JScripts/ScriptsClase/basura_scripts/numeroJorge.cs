using UnityEngine;

public class numeroJorge : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Numero Jorge");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
